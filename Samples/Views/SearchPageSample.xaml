<?xml version="1.0" encoding="UTF-8"?>
<customrenderers:SearchPage xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		xmlns:customrenderers="clr-namespace:Samples.CustomRenderers;assembly=Samples" 
		x:Class="Samples.Views.SearchPageSample"
		SearchPlaceHolderText="Type to Search"
		SearchCommand="{Binding SearchCommand}" SearchText="{Binding TextSearch, Mode=TwoWay}">
	<StackLayout Spacing="20">
    <ListView
            x:Name="listaExclusiva"
            ItemsSource="{Binding Lista}"
            SeparatorVisibility="None"
            BackgroundColor="#EEEEEE"
            HasUnevenRows="true">
    </ListView>
	</StackLayout>
</customrenderers:SearchPage>